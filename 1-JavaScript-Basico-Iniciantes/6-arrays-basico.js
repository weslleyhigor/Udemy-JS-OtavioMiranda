const alunos = ['Higor', 'Joao', 'Maria'];


//alunos.unshift('Luiza'); // Adiciona no inicio
//alunos.unshift('Fábio'); // Adiciona no inicio

//alunos.push('Luiza'); // Adiciona no fim
//alunos.push('Fábio'); // Adiciona no fim

//alunos[alunos.length] = 'Luiza'; // Adiciona no fim
//alunos[alunos.length] = 'Fávio'; // Adiciona no fim

//alunos.pop(); // Remove o último elemento da array
//const removido = alunos.pop(); // Guarda o último elemento da array que foi removido, dentro de uma variável

//alunos.shift(); // Remove o primeiro elemento da array
//const removido = alunos.shift(); // Guarda o primeiro elemento da array que foi removido, dentro de uma variável

//delete(alunos[1]); // Remove o elemento sem mexer nas estrutas de indices da array. O indice 1 ficará vazio, porem, existindo.

//console.log(alunos.slice(0, alunos.length)) // Mostra os elementos da array até o ultimo indice

//alunos.splice(2, 1) ;
// O splice, remove 1 valor do indice 2, mas pode remover mais indices alterando o 1 para 2 ou 3...
// Pode ser colocado outro valor no lugar do indice 2 ficando assim:  alunos.splice(2, 1, 'Raimundo');
// Pode também não remover nenhum, apenas colocar na frente do indice 2 inserindo mais 1 elemento
// alunos.splice(2, 0, 'Raimundo'); 

console.log(alunos);