//Strings
const nomeUm = 'Higor';
const nomeDois = "Higor";
const nomeTres = `Higor`;
console.log(typeof(nomeUm));
console.log(typeof(nomeDois));
console.log(typeof(nomeTres));
//Number
const numUm =  10;
const numDois = 10.52;
console.log(typeof(numUm));
console.log(typeof(numDois));

//Undefined
let nome; //Undefined >> Nao aponta pra local nenhum na memoria
console.log(typeof(nome));

//Null
const sobrenome = null; //Null >> Nao aponta pra local nenhum na memoria
console.log(typeof(sobrenome)); // retorna um valor do tipo object

//Boolean
const boolean = true; //Boolean = true, false (lógico)
console.log(typeof(boolean));